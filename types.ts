export type Todo = {
    id: number;
    title: string;
    isDone: boolean;
};
export type NewTodo = {
    title: string;
    isDone: boolean;
};
export type UpdatedTodo = {
    title: string;
    isDone: boolean;
};

